# 敦煌迷途 - 部署配置
PROJECT_NAME="maze"
DOMAIN="maze.mplusm.site"

# 前端配置（静态Web导出）
FRONTEND_TYPE="static"
FRONTEND_BUILD_DIR="web"
FRONTEND_BUILD_CMD=""

# 后端配置（无后端）
BACKEND_TYPE="none"
BACKEND_PORT=""
BACKEND_START_CMD=""
API_PREFIX=""

# 服务器配置
SERVER_IP="43.133.145.77"
SERVER_USER="root"
WEB_ROOT="/www/wwwroot/maze.mplusm.site"

# SSL配置
SSL_TYPE="letsencrypt"
SSL_EMAIL="admin@mplusm.site"

# 环境变量
ENV_VARS=""
